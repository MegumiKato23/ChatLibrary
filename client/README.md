# Chat Library Frontend

基于 Nuxt 4 + Pinia + Element Plus 构建的现代化 ChatPDF 前端应用，支持多格式文档解析与智能对话。

## ✨ 功能特性

### 👤 用户系统

- **安全认证**: 登录/注册（JWT + Cookie），自动状态持久化。
- **信息管理**: 个人信息修改、密码修改（支持强度检测）。
- **交互体验**: 统一的弹窗风格，平滑过渡动画，实时表单验证。

### 📄 文档管理

- **多格式支持**: PDF, DOCX, DOC, TXT, MD。
- **在线预览**:
  - **PDF**: 使用 `@vue-office/pdf` 高清预览。
  - **DOCX**: 使用 `@vue-office/docx` 还原排版。
  - **DOC/TXT**: 后端解析 HTML/文本内容渲染。
- **文件操作**: 拖拽上传、列表管理、删除确认。

### 💬 智能对话 (RAG)

- **流式响应**: 基于 SSE (Server-Sent Events) 的实时 AI 回复。
- **会话管理**: 多会话创建、切换、历史记录查看、删除。
- **上下文**: 基于当前选中文档的上下文问答。

### 🎨 界面设计

- **响应式布局**: 适配移动端与桌面端的现代化三栏布局。
- **主题风格**: 简约清爽的 UI，Element Plus + Tailwind CSS 定制。

## 🛠️ 技术栈

- **框架**: [Nuxt 4](https://nuxt.com) (Vue 3)
- **状态管理**: [Pinia](https://pinia.vuejs.org)
- **UI 组件库**: [Element Plus](https://element-plus.org)
- **样式**: [Tailwind CSS](https://tailwindcss.com) + SCSS
- **文档预览**: [VueOffice](https://github.com/501351981/vue-office)
- **HTTP**: `ofetch` 封装 (自动拦截/重试)

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- pnpm (推荐) / npm / yarn

### 安装与运行

1. **安装依赖**

   ```bash
   pnpm install
   ```

2. **配置环境变量**
   默认连接本地后端 `http://localhost:8080`。
   如需修改，请在 `nuxt.config.ts` 中配置 `runtimeConfig.public.apiBase`。

3. **启动开发服务器**

   ```bash
   pnpm dev
   ```

   访问 `http://localhost:3000`。

4. **构建生产版本**
   ```bash
   pnpm build
   ```

## 📂 项目结构

```
chat-library-frontend/
├── app/
│   ├── components/         # Vue 组件 (Login, User, DocumentPreview, Sidebar...)
│   ├── composables/        # 组合式函数 (API 封装)
│   ├── layouts/            # 页面布局
│   ├── pages/              # 页面路由
│   ├── stores/             # Pinia 状态仓库
│   └── types/              # TypeScript 类型定义
├── public/                 # 静态资源
├── nuxt.config.ts          # Nuxt 配置
└── package.json
```

## 🔌 API 模块

位于 `app/composables/api/`：

- **useUserApi**: 用户登录、注册、更新、密码修改。
- **useDocumentApi**: 文档上传、列表、预览内容获取。
- **useChatApi**: 会话创建、历史记录、SSE 消息发送。

## 📄 License

MIT
