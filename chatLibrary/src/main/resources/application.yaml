spring:
  application:
    name: chatLibrary
  ai:
    ollama:
      init:
        pull-model-strategy: always
        timeout: 60s
        max-retries: 3
        embedding:
          additional-models: bge-base-zh-v1.5
      base-url: http://localhost:11434
      chat:
        model: deepseek-r1:14b
    vectorstore:
      qdrant:
        initialize-schema: true
  r2dbc:
    url: r2dbc:mysql://localhost:3306/chat_ai_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  sql:
    init:
      mode: always
  main:
    allow-bean-definition-overriding: true
    web-application-type: reactive

logging:
  level:
    org.springframework.ai: debug
    com.zg.ai: debug
